# シンプル オセロ

ReactとViteで動作する、プレイヤー対CPUのオセロゲームです。

## 必要な環境

- Node.js 20.19以降、または22.12以降
- npm

## 起動方法

```sh
npm install
npm run dev
```

ターミナルに表示されたURL（通常は `http://localhost:5173`）をブラウザで開いてください。

## 本番用ビルド

```sh
npm run build
```

ビルド結果は `dist` ディレクトリに生成されます。
